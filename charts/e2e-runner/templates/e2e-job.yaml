{{- $fullname := include "e2e-runner.fullname" . }}
{{- $labels := include "e2e-runner.labels" . }}

{{- if eq .Values.job.kind "CronJob" }}
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ $fullname }}
  labels:
    {{- $labels | nindent 4 }}
spec:
  schedule: {{ .Values.job.cronSchedule | quote }}
  concurrencyPolicy: {{ .Values.job.concurrencyPolicy }}
  successfulJobsHistoryLimit: {{ .Values.job.successfulJobsHistoryLimit }}
  failedJobsHistoryLimit: {{ .Values.job.failedJobsHistoryLimit }}
  jobTemplate:
    spec:
      {{- include "e2e-runner.jobSpec" . | nindent 6 }}
{{- else }}
---
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ $fullname }}-{{ now | unixEpoch }}
  labels:
    {{- $labels | nindent 4 }}
  {{- if .Values.helmTest.enabled }}
  annotations:
    {{- range $key, $value := .Values.helmTest.annotations }}
    {{ $key }}: {{ $value | quote }}
    {{- end }}
  {{- end }}
spec:
  {{- include "e2e-runner.jobSpec" . | nindent 2 }}
{{- end }}

{{/*
Job Spec 模板 (供 Job 和 CronJob 复用)
注意：define 块内需要重新计算变量，无法使用外部定义的 $fullname 等
*/}}
{{- define "e2e-runner.jobSpec" -}}
ttlSecondsAfterFinished: 86400  # 24小时后自动清理
backoffLimit: {{ .Values.job.backoffLimit }}
parallelism: {{ .Values.job.parallelism }}
completions: {{ .Values.job.completions }}
{{- if .Values.job.activeDeadlineSeconds }}
activeDeadlineSeconds: {{ .Values.job.activeDeadlineSeconds }}
{{- end }}
template:
  metadata:
    labels:
      {{- include "e2e-runner.selectorLabels" . | nindent 6 }}
  spec:
    restartPolicy: Never
    
    {{- with .Values.job.nodeSelector }}
    nodeSelector:
      {{- toYaml . | nindent 8 }}
    {{- end }}
    
    {{- with .Values.job.affinity }}
    affinity:
      {{- toYaml . | nindent 8 }}
    {{- end }}
    
    {{- with .Values.job.tolerations }}
    tolerations:
      {{- toYaml . | nindent 8 }}
    {{- end }}
    
    initContainers:
      # 初始化容器：在 workdir 下创建必要的输出目录结构
      - name: init-dirs
        image: busybox:1.36
        command:
          - /bin/sh
          - -c
          - |
            mkdir -p {{ .Values.job.workdir }}/{{ .Values.persistence.outPutPath }}
            echo "Directories initialized"
        volumeMounts:
          - name: artifacts
            mountPath: {{ .Values.job.workdir }}/{{ .Values.persistence.outPutPath }}
            subPath: {{ .Values.persistence.outPutPath }}
    
    containers:
      - name: e2e-runner
        image: "{{ .Values.job.image.repository }}:{{ .Values.job.image.tag }}"
        imagePullPolicy: {{ .Values.job.image.pullPolicy }}
        workingDir: {{ .Values.job.workdir }}
        
        command:
          - /bin/bash
          - /scripts/run-tests.sh
        
        env:          
          # 从 values 传入的环境变量
          {{- range $key, $value := .Values.env }}
          - name: {{ $key }}
            value: {{ $value | quote }}
          {{- end }}
        
        resources:
          {{- toYaml .Values.job.resources | nindent 10 }}
        
        volumeMounts:
          # 挂载 PVC：subPath → workdir 内的输出目录，与 pytest.ini 路径对齐
          - name: artifacts
            mountPath: {{ .Values.job.workdir }}/{{ .Values.persistence.outPutPath }}
            subPath: {{ .Values.persistence.outPutPath }}
          
          # 挂载 ConfigMap 中的脚本、pytest.ini 和环境配置
          - name: config
            mountPath: /scripts/run-tests.sh
            subPath: run-tests.sh
          - name: config
            mountPath: {{ .Values.job.workdir }}/pytest.ini
            subPath: pytest.ini
          - name: config
            mountPath: {{ .Values.job.workdir }}/configs/env.{{ .Values.envConfig.environment }}.yaml
            subPath: env.{{ .Values.envConfig.environment }}.yaml
    
    volumes:
      # PVC 存储卷
      - name: artifacts
        {{- if .Values.persistence.enabled }}
        persistentVolumeClaim:
          claimName: {{ include "e2e-runner.pvcName" . }}
        {{- else }}
        emptyDir: {}
        {{- end }}
      
      # ConfigMap 配置卷
      - name: config
        configMap:
          name: {{ include "e2e-runner.fullname" . }}-config
          defaultMode: 0755
{{- end }}
