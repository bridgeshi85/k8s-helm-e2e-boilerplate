kube-prometheus-stack:
  prometheus:
    prometheusSpec:
      serviceMonitorSelectorNilUsesHelmValues: false
  
  grafana:
    sidecar:
      dashboards:
        enabled: true
        label: grafana_dashboard
    adminPassword: "admin"
    
  alertmanager:
    enabled: false